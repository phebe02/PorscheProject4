
<!DOCTYPE html>
<html>
<head>
  <title>Fabriek Bewerken</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body class="bg-light p-4">
  <div class="container">
    <div class="card shadow-lg">
      <div class="card-header bg-warning">
        ✏️ Bewerken: <%= factory.name %>
      </div>
      <div class="card-body">
        <form method="POST" action="/edit-factory">
          <input type="hidden" name="id" value="<%= factory.id %>">
          <div class="mb-3">
            <label class="form-label">Naam</label>
            <input class="form-control" name="name" value="<%= factory.name %>" required>
          </div>
          <div class="mb-3">
            <label class="form-label">Locatie</label>
            <input class="form-control" name="location" value="<%= factory.location %>" required>
          </div>
          <div class="mb-3">
            <label class="form-label">Oprichtingsjaar</label>
            <input type="number" class="form-control" name="founded" value="<%= factory.founded %>" min="1900" required>
          </div>
          <div class="mb-3">
            <label class="form-label">Elektrisch gecertificeerd</label>
            <select class="form-select" name="isElectricCertified" required>
              <option value="true" <%= factory.isElectricCertified ? "selected" : "" %>>Ja</option>
              <option value="false" <%= !factory.isElectricCertified ? "selected" : "" %>>Nee</option>
            </select>
          </div>
          <button class="btn btn-success" type="submit">💾 Opslaan</button>
          <a href="/factory?id=<%= factory.id %>" class="btn btn-secondary">Annuleer</a>
        </form>
      </div>
    </div>
  </div>
</body>
</html>
