
<!DOCTYPE html>
<html>
<head>
  <title>Porsche Bewerken</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body class="bg-light p-4">
  <div class="container">
    <div class="card shadow-lg">
      <div class="card-header bg-primary text-white">
        ✏️ Bewerken: <%= porsche.name %>
      </div>
      <div class="card-body">
        <form method="POST" action="/edit">
          <input type="hidden" name="id" value="<%= porsche.id %>">
          <div class="mb-3">
            <label class="form-label">Naam</label>
            <input class="form-control" name="name" value="<%= porsche.name %>" required>
          </div>
          <div class="mb-3">
            <label class="form-label">Prijs (€)</label>
            <input type="number" class="form-control" name="price" value="<%= porsche.price %>" min="0" required>
          </div>
          <div class="mb-3">
            <label class="form-label">Categorie</label>
            <select class="form-select" name="category" required>
              <option value="">-- Kies categorie --</option>
              <option value="Coupé" <%= porsche.category === "Coupé" ? "selected" : "" %>>Coupé</option>
              <option value="SUV" <%= porsche.category === "SUV" ? "selected" : "" %>>SUV</option>
              <option value="Elektrisch" <%= porsche.category === "Elektrisch" ? "selected" : "" %>>Elektrisch</option>
              <option value="Cabrio" <%= porsche.category === "Cabrio" ? "selected" : "" %>>Cabrio</option>
            </select>
          </div>
          <div class="form-check mb-3">
            <input class="form-check-input" type="checkbox" name="isAvailable" <%= porsche.isAvailable ? "checked" : "" %>>
            <label class="form-check-label">Beschikbaar</label>
          </div>
          <button class="btn btn-success" type="submit">💾 Opslaan</button>
          <a href="/detail?id=<%= porsche.id %>" class="btn btn-secondary">Annuleer</a>
        </form>
      </div>
    </div>
  </div>
</body>
</html>
