
<!DOCTYPE html>
<html>
<head>
  <title>Fabriek Detail</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
  
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark px-3">
    <a class="navbar-brand" href="/">Porsche App</a>
    <div class="collapse navbar-collapse">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item"><a class="nav-link" href="/">Porsches</a></li>
        <li class="nav-item"><a class="nav-link" href="/factories">Fabrieken</a></li>
      </ul>
      <ul class="navbar-nav">
        <% if (user && user.role === "ADMIN") { %>
          <li class="nav-item"><a class="nav-link" href="/logout">Logout</a></li>
        <% } else { %>
          <li class="nav-item"><a class="nav-link" href="/register">Registreren</a></li>
        <% } %>
      </ul>
    </div>
  </nav>



  <div class="container mt-4">
    <a href="/factories" class="btn btn-secondary mb-3">⬅ Terug naar overzicht</a>

    <div class="card">
      <div class="row g-0">
        <div class="col-md-4 text-center p-3">
          <img src="<%= factory.logoUrl %>" alt="Factory logo" class="img-fluid rounded-start" style="max-width: 200px;">
        </div>
        <div class="col-md-8">
          <div class="card-body">
            <h2 class="card-title"><%= factory.name %></h2>
            <p class="card-text"><strong>Locatie:</strong> <%= factory.location %></p>
            <p class="card-text"><strong>Opgericht in:</strong> <%= factory.founded %></p>
            <p class="card-text"><strong>Elektrisch gecertificeerd:</strong> <%= factory.isElectricCertified ? "✅" : "❌" %></p>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
